# Architecture Decision Records

MADR-style ADRs recording ichiflow's locked architecture decisions (see
[../architecture/BRIEF.md](../architecture/BRIEF.md)). Each ADR cites its source research doc(s) in
[../research/](../research/) and records honest downsides, not just the win.

| ADR | Decision | Status |
|---|---|---|
| [0001](0001-canonical-rule-representation-dmn.md) | Canonical rule representation is DMN 1.6 (DRD + FEEL); engine-native formats are quarantined escape hatches; interchange caveats mitigated | accepted |
| [0002](0002-pluggable-decision-engine-spi-drools-default.md) | Pluggable Decision Engine SPI; Apache KIE/Drools default (JVM), GoRules ZEN second (TS/edge); vs Drools-monoculture and ODM | accepted |
| [0003](0003-temporal-durable-execution-substrate.md) | Temporal (MIT) as the durable-execution substrate; vs Camunda 8 licensing, Flowable fallback, Kogito/Restate/DBOS/Conductor | accepted |
| [0004](0004-declarative-flow-dsl-on-temporal.md) | Declarative CNCF-Serverless-Workflow-aligned Flow DSL interpreted on Temporal; vs raw SDK and BPMN-first; amended with first-class `compute` code-activity step + one-way typed flow builder | accepted (amended 2026-07-12) |
| [0005](0005-first-party-case-and-human-task-module.md) | First-party Case & Human-Task module; assignment routing is a Decision; vs BPMN engines' built-ins | accepted |
| [0006](0006-typespec-authoring-openapi-jsonschema-canonical.md) | TypeSpec authoring; emitted OpenAPI 3.1 / JSON Schema 2020-12 are the contract of record; TypeSpec swappable | accepted |
| [0007](0007-kotlin-core-typescript-edges.md) | Kotlin core, TypeScript edges; generated types both sides; Temporal Kotlin = Java-SDK-extension caveat | accepted |
| [0008](0008-jsonforms-model-ui-overrides.md) | JSON Forms model: independent uischema + tester/priority renderer registry; vs rjsf, scaffold-then-eject, app builders | accepted |
| [0009](0009-identity-broker-per-audience.md) | Identity broker per audience (Keycloak primary, Zitadel B2B2C, Better Auth on TS edges, RFC 8693 propagation) | accepted |
| [0010](0010-hybrid-authorization-openfga-plus-policy.md) | Hybrid authorization: OpenFGA ReBAC backbone + Cedar/OPA ABAC; one PDP for API and UI; decision logs; amended — v1 = OpenFGA only, Cedar/OPA ABAC is a post-v1/Enterprise add-on behind the same PDP interface | accepted (amended 2026-07-12) |
| [0011](0011-decisionrecord-and-selective-event-sourcing.md) | Per-case DecisionRecord + "why" API; event-source core only; outbox elsewhere; bitemporal as-of; amended — observability is OTel-native, BYO backend (all signals via OTLP; no proprietary store; no bundled Grafana-class stack; Dev bundles a minimal local viewer) | accepted (amended 2026-07-12) |
| [0012](0012-postgresql-first-storage-spis.md) | PostgreSQL-first storage with SPIs for audit/search/analytics; multi-DB correlation via case_id | accepted |
| [0013](0013-modular-monolith-split-later.md) | Modular monolith by default, async-first boundaries, split later; tier ladder; zone separation from day one | accepted |
| [0014](0014-map-first-migrate-last.md) | Map first, migrate last: three-ring brownfield model; Migration Copilot guardrails; decision parity testing; exit story | accepted |
| [0015](0015-first-party-mcp-server-and-agent-kit.md) | First-party ichiflow-mcp server + in-repo agent kit; three server-enforced guardrail tiers; agents as NHIs | accepted |
| [0016](0016-license-hygiene-policy.md) | License hygiene: Apache-2.0/MIT substrate preference; the specific avoid-list and why; CI license allowlist; consistency note — ichiflow itself is fully OSS ([0022](0022-fully-open-source.md)) | accepted |
| [0017](0017-v1-kernel-and-governance-dial.md) | The v1 kernel cut (schema + decisions + flows/cases + entity store + one portal + basic adapters + why API + dev tier); governance dial defaults (Dev=off / Team=light / Enterprise=full); Copilots + compliance profile post-v1; amended — kernel is single-org per deployment (multi-tenant seams designed now) and v1 acceptance = two required exercises (reference product end-to-end + migration in/out with parity testing) | accepted (amended 2026-07-12) |
| [0018](0018-domain-entity-store.md) | Domain entity store: schema-defined entities, PostgreSQL-first generated repositories + query/pagination/search contract; CRUD+outbox not event-sourced; ORM choice open | accepted |
| [0019](0019-ai-chat-first-authoring.md) | AI-chat-first authoring for all personas; read-only live-preview projections to judge; no drag-and-drop/visual-builder canvases; direct artifact editing stays for developers; amended — designer flow is prompt-first (playground is THE surface), Figma optional (tokens in / screens out, no hi-fi round-trip bridge) | accepted (amended 2026-07-12) |
| [0020](0020-prod-access-posture-dial.md) | Production-access posture is a configurable dial (zero-direct-access / agents-mediated-humans-conventional / custom); ichiflow ships the mediation layers (why API, ops console, MCP guardrail tiers, env promotion, loud/logged break-glass); vs one forced default | accepted |
| [0021](0021-reporting-via-oss-bi.md) | Reporting = embed proven OSS BI (Metabase/Superset-class) over governed read models (cases/outcomes/conditions/SLAs/decision stats); SSO via broker + row/field security via the same PDP; no custom report engine; "prefer proven open source" principle | accepted |
| [0022](0022-fully-open-source.md) | ichiflow is fully open source (Apache-2.0/MIT), all of it incl. "enterprise"/"compliance"; tiers are technical capability profiles not commercial editions; compliance profile is an OSS optional install; monetize services not features; vs open-core / source-available (rejected); consistent with [0016](0016-license-hygiene-policy.md) | accepted |
| [0023](0023-public-sector-first.md) | Design-target first adopter is government / public sector (permitting/licensing/benefits/inspections); regulated finance is the adjacent second; permit walkthrough is the canonical reference product; procurement realities fit; no real systems named; vs finance-first / generic breadth | accepted |
| [0024](0024-llm-only-internal-surfaces-v1.md) | LLM-only internal surfaces for v1: v1 builds human UI only for the generated end-user Portals (customer/partner + back-office manual-review inbox/case view); every internal/operator/builder/admin surface (support/ops console, governance/approval, playground/workbench, admin/config, auditor query) is served by Claude Code + ichiflow-mcp + CLI + chat with read-only `ichiflow preview` artifacts; third-party OSS surfaces unaffected; the typed APIs/MCP tools are the seam so post-v1 builder UIs are additive clients; vs building the internal apps in v1 / hybrid | accepted |
| [0025](0025-reference-data-ownership-and-teams.md) | Reference data as interdependent, owned, governed assets (CodeSet `codeRef` links + cross-version referential integrity + dependency graph + publish-time impact analysis; owning Team + stewards; approval-as-a-Flow extended to CodeSets, routed by owning-team role as a Decision, per-artifact governance override) and Teams as first-class sub-org structure (OpenFGA relations; design-time + runtime through one PDP); vs flat ungoverned lookup tables / external MDM / tenant-per-team; amended — env-pin **activation** may be partitioned **per owning Team** (`environments/prod/<team>.pins.yaml`) so Teams release independently while the deployment composes them, without crossing the tenancy line | accepted (amended 2026-07-12) |
| [0026](0026-harness-first-construction.md) | Harness-first construction: every subsystem ships a harness (fixtures + executable checks + JSON verdict + enumerable progress) before its implementation; single `ichiflow verify` entry point + verdict schema + progress ledger (`get_verify_status`); per-subsystem catalog incl. the Decision-SPI DMN-TCK conformance suite any engine must pass and MCP tier-enforcement tests; harnesses are governed Workspace artifacts that ship as a product feature; scoped verify on write via hooks, full in CI; retry-forbidden flake policy; build ichiflow harness-first with each phase's exit = its harness green; the two v1 acceptance exercises are the outermost harnesses; vs test-after / prose self-assessment / human-QA-primary | accepted |
| [0027](0027-dmn-authoring-projection.md) | LLM-friendly **decision source** projection: a structured markdown/YAML/JSON form (FEEL throughout) expresses the **full DMN 1.6 feature set** (DRDs, all boxed expressions, item definitions) and compiles deterministically **one-way** to DMN 1.6 XML, which stays the executed/exported/interchange artifact; mirrors TypeSpec→OpenAPI and flow-builder→FlowJSON; **100% AI coverage is a verified metric via a projection-coverage harness** (doc 13 §2.b); `authored-in` extends to DecisionModels (`decision-source \| dmn-xml \| drl \| ai-chat`); **DRL/rule-units/CEP escape hatches are first-class AI-authorable + testable governed paths** (schema'd wrapper, compile-check in verify, simulation, trace, provenance) while quarantine stays a *portability* marker only; vs table-only projection / DMN-XML-only / JDM-as-source / import-only escape hatches | accepted |
| [0028](0028-delegation-step.md) | Canonical **`external-task` (delegation) step**: submit a schema'd request through an outbound Adapter → durably await a **correlated** response through an inbound Adapter → validate → resume (timeout/escalation/compensation) — the **machine analog of the human `Task`** (await-signal + pausable SLA), reusing that machinery; *which* external system is itself a Decision (mirror of assignment routing); declarative **correlation contract** + at-least-once/idempotent-receiver/dedup-on-response; failure taxonomy (no-response timeout / negative-ack / malformed → DLQ + Case surfacing); transport-pluggable under one interface via the **request-reply EIP** adapter bindings — HTTP sync/callback/polling + message-queue request-reply (v1) and an **SFTP file round-trip** (designed now, built post-v1); conformance vectors vs a mock external system (doc 13 §2.c); vs plain adapter-call + separate wait step / raw compute step / human-task misuse / extension step type | accepted |
| [0029](0029-document-issuance.md) | **Document issuance** as a first-class, API-first, extensible capability: **`Document`** (immutable, versioned, issued from a data snapshot + Outcome; allocated reference number with **gap-free/gapped** semantics; verification hash + data-minimal **public verification endpoint**; lifecycle issued→superseded→revoked + optional **offer accept/decline** facet in the Flow, incl. **multi-party countersignature**; **binary is derived** → object-storage SPI + crypto-shredding), **`doctemplate`** (new governed designer artifact class, sibling of uischema/pageschema/copyset; Design-Kit AI-chat authoring; **pluggable rendering-engine SPI**, **Typst** default / WeasyPrint alt / Chromium non-default; PDF/UA; i18n via copyset/CodeSet), and canonical **`issue-document`** step (owns exactly-once-memoized allocation + lifecycle; pure render dispatched beneath via the SPI; delivery composes with notification adapters); **three placement profiles** (core audit-spine semantics always shipped / default rendering an optional swappable SPI component / **external delegation** of rendering or full issuance to an enterprise CCM — registry+lifecycle+verification always stay in ichiflow); harness §2.k (render determinism / verification vectors / lifecycle + replay idempotency); reconciles the case studies' pre-normative nouns; vs adapter-call/compute/extension-step modelling, renderer-baked-into-core, Chromium-default, binary-as-truth | accepted |
| [0030](0030-quota-ledger.md) | **`QuotaLedger`** — a first-class multi-dimensional resource-ledger artifact (Team-owned, effective-dated, CodeSet-sibling): declared **dimensions** + CodeSet-like **capacity rows**; **invariants** (`headroom >= 0`, `committed <= capacity`) enforced **at the ledger atomically**, not in hand-written SQL; atomic **`reserve`/`commit`/`release`** from a canonical **`quota-op`** Flow step with TTL'd reservations, exactly-once-memoized on replay like `issue-document` allocation (ADR-0029); **monetary** amounts as a dimension kind (variable-size reservation → grants budget pools); **ranked reserve-list draw** + declared **release-back reflow policy**; every movement an audited DecisionRecord event; concurrency/contention harness (§2.l); replaces the case studies' interim `compute`-ref modeling; confirmed by ballot G2 + grants G3; vs ad-hoc SQL / CodeSet / DMN-over-headroom | accepted |
| [0031](0031-set-level-cases.md) | **Set-level Cases** — two shapes on one design: **`cohort`** (a gather-barrier over a case selector → one set-level Decision/compute → scatter; bounded fan-in; a **cohort-scoped DecisionRecord** keyed by `cohortId` that member Cases reference — fan-in to one decision, composes with the QuotaLedger ranked draw) and **`bundle`** (a computed **heterogeneous** sub-Case fan-out, per-member `caseType` via the CaseType catalog, joined by a **partial-tolerant status roll-up** — explicitly **not** a `CompositeOutcome`; parent record **references** child records — fan-out to N independent decisions); resolves ballot G4 + licensing G2; vs `batch` trigger / `CompositeOutcome` / one unified primitive | accepted |
| [0032](0032-case-association.md) | **`Case association`** — first-class **many-to-many peer** links between otherwise-independent Cases: typed **link kinds** (`investigation-group`, `portfolio-of-applicant`, `duplicate-suspect`); **PDP-scoped visibility** (read across linked Cases without collapsing their ownership/audit boundaries); audited **link/unlink** with its own DecisionRecord; **cross-Case invariant checks** (double-funding, over-cap) over associations + the entity store; **distinct** from parent/child correlation (appeal) and from bundles (a bundle owns its children; an association links peers); resolves insurance gap 1 + grants G1; vs ad-hoc queries / reuse correlation / reuse bundle / untyped edge | accepted |
| [0033](0033-packaging-and-placement.md) | **Packaging & placement doctrine** — generalizes ADR-0029's three profiles into a placement **decision tree**: (i) audit-spine-dependent semantics → **core**; (ii) ichiflow-differentiating → **first-party optional component**; (iii) commodity w/ mature OSS → **SPI + thin default + integration guidance**; (iv) enterprise-already-owns-one → **designed external-delegation path**; classifies **semantics not product areas** (most capabilities are hybrid); a **classification table** across rendering / notifications / BI / observability / identity / authz / rule engine / workflow substrate / entity+object storage / migration / MCP (living copy in doc 12 §6); unifies ADR-0002/0009/0010/0011/0021/0029; vs per-ADR intuition / monolithic classification / maximize-core / minimize-core | accepted |

{
  "name": "@ichiflow/flow",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Phase 3 flow layer — the generic Temporal interpreter over the flow-JSON DSL (ADR-0003/0004). Phase 3.0 is the interpreter-determinism spike: replay-twice determinism + SLA time-skip.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "vitest run",
    "spike": "node dist/spike.js"
  },
  "dependencies": {
    "@temporalio/workflow": "1.11.7"
  },
  "devDependencies": {
    "@eslint/js": "9.15.0",
    "@temporalio/activity": "1.11.7",
    "@temporalio/client": "1.11.7",
    "@temporalio/testing": "1.11.7",
    "@temporalio/worker": "1.11.7",
    "@types/node": "20.17.6",
    "eslint": "9.15.0",
    "typescript": "5.6.3",
    "typescript-eslint": "8.18.0",
    "vitest": "2.1.5"
  }
}

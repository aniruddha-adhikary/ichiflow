{
  "name": "@ichiflow/issuance",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Phase 5.3 Document issuance core + rendering SPI: deterministic Typst-default binding, exactly-once numbering, lifecycle, and verification.",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "vitest run",
    "preview": "node dist/preview.js"
  },
  "devDependencies": {
    "@eslint/js": "9.15.0",
    "@types/node": "20.17.6",
    "eslint": "9.15.0",
    "typescript": "5.6.3",
    "typescript-eslint": "8.18.0",
    "vitest": "2.1.5"
  }
}

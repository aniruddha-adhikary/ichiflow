{
  "name": "decision-eval DECLINE branch — sub-threshold score annotates the blackboard with 0",
  "flow": {
    "id": "decision-decline",
    "schemaVersion": "flow/v1",
    "input": { "vars": { "score": 20 } },
    "steps": [
      {
        "id": "s1",
        "type": "decision-eval",
        "decision": "threshold-approval",
        "in": ["score"],
        "out": "decision"
      }
    ]
  },
  "expect": { "vars": { "score": 20, "decision": 0 }, "steps": 1, "slaMs": 0 }
}

{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "ComputeStep.json",
    "type": "object",
    "properties": {
        "type": {
            "type": "string",
            "const": "compute"
        },
        "op": {
            "$ref": "ComputeOp.json"
        }
    },
    "required": [
        "type",
        "op"
    ],
    "allOf": [
        {
            "$ref": "FlowStep.json"
        }
    ],
    "description": "A `compute` step — a pure code-activity that reshapes inter-step data (doc 04 §2.6). Runs as an\nactivity (the non-determinism boundary), so the interpreter body stays deterministic."
}
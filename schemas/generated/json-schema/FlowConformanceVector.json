{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "FlowConformanceVector.json",
    "type": "object",
    "properties": {
        "name": {
            "type": "string",
            "description": "Human-readable vector name; doubles as intent documentation."
        },
        "flow": {
            "$ref": "Flow.json"
        },
        "signals": {
            "type": "array",
            "items": {
                "$ref": "FlowSignal.json"
            },
            "description": "Resolution signals delivered to `human-task` steps during interpretation; omit to exercise the SLA-escalation path."
        },
        "replies": {
            "type": "array",
            "items": {
                "$ref": "ExternalReply.json"
            },
            "description": "Correlated inbound replies delivered to `external-task` steps during interpretation (doc 04 §2.8); omit to exercise the delegation timeout/escalation path."
        },
        "expect": {
            "$ref": "FlowExpectation.json"
        }
    },
    "required": [
        "name",
        "flow",
        "expect"
    ],
    "description": "One **conformance vector** (build plan 3.1–3.2 exit criterion). Pairs a DSL-valid Flow — plus any\nscripted resolution `signals` — with the final blackboard the generic interpreter must produce, an\nindependently-pinned oracle proving the interpreter walks/orders/dispatches/awaits correctly rather\nthan merely being self-consistent."
}
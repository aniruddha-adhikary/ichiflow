{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "FlowExpectation.json",
    "type": "object",
    "properties": {
        "vars": {
            "$ref": "#/$defs/RecordFloat64",
            "description": "The final blackboard the interpreter must return: variable name → value."
        },
        "steps": {
            "type": "integer",
            "minimum": -2147483648,
            "maximum": 2147483647,
            "description": "The number of steps the flow declares (guards vector/flow drift)."
        },
        "slaMs": {
            "type": "integer",
            "description": "Total scheduled SLA/timer duration in milliseconds across the flow (timer steps + escalated human-tasks)."
        },
        "events": {
            "type": "array",
            "items": {
                "type": "string"
            },
            "description": "Optional pinned Case/Task **event-history** type sequence (doc 04 §5.1/§5.2), e.g. `[\"case.created\", \"task.created\", …]`."
        }
    },
    "required": [
        "vars",
        "steps",
        "slaMs"
    ],
    "description": "The pinned expected observations of interpreting a conformance vector's Flow.",
    "$defs": {
        "RecordFloat64": {
            "type": "object",
            "properties": {},
            "unevaluatedProperties": {
                "type": "number"
            }
        }
    }
}
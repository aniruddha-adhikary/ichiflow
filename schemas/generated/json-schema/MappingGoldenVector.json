{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "MappingGoldenVector.json",
    "type": "object",
    "properties": {
        "name": {
            "type": "string",
            "description": "Human-readable vector name; doubles as intent documentation."
        },
        "mappingId": {
            "type": "string",
            "description": "The `Mapping.id` under test."
        },
        "wire": {
            "$ref": "#/$defs/RecordUnknown",
            "description": "The decoded inbound wire message."
        },
        "expected": {
            "$ref": "CanonicalEnvelope.json",
            "description": "The canonical envelope the mapping must deterministically produce."
        }
    },
    "required": [
        "name",
        "mappingId",
        "wire",
        "expected"
    ],
    "description": "One **mapping golden vector** (doc 13 §2.d \"mapping golden files\") — an `input wire message` paired\nwith the `expectedCanonical` envelope the pure mapping must produce. A mapping change that alters an\noutput shows up as a golden diff here.",
    "$defs": {
        "RecordUnknown": {
            "type": "object",
            "properties": {},
            "unevaluatedProperties": {}
        }
    }
}